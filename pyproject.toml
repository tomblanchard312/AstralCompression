[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "astral-compression"
version = "0.4.0"
requires-python = ">=3.9"
dependencies = ["reedsolo>=1.7"]

[project.scripts]
astral = "astral.cli:main"
